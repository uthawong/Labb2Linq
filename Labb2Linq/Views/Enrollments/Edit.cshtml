@model Labb2Linq.Models.Enrollment

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Enrollment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="EnrollmentId" />
            <div class="form-group">
                <label asp-for="FkStudentId" class="control-label">Student</label>    @* //döpt om *@
                <select asp-for="FkStudentId" class="form-control" asp-items="ViewBag.FkStudentId"></select>
                <span asp-validation-for="FkStudentId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FkCourseId" class="control-label">Course</label>   @* //döpt om *@
                <select asp-for="FkCourseId" class="form-control" asp-items="ViewBag.FkCourseId"></select>
                <span asp-validation-for="FkCourseId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FkTeacherId" class="control-label">Teacher</label>   @* //döpt om *@
                <select asp-for="FkTeacherId" class="form-control" asp-items="ViewBag.FkTeacherId"></select>
                <span asp-validation-for="FkTeacherId" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">  @* //lagt till mt-3, margin top  -  alt. mb-3, margin bottom 3// *@
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
